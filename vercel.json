{
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["index.html", "*.png", "*.jpeg", "*.jpg", "*.mp3"]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
